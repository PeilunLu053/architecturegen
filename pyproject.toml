[project]
name = "img2cad"
version = "0.1.0"
description = "Image to CAD conversion toolkit"
authors = [{ name = "AutoGenerated" }]
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
pillow = ["pillow>=9.0"]
dev = ["pytest>=7.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
